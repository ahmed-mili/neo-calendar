<?xml version="1.0" encoding="utf-8"?>
<!-- La carte du popup de mise a jour. Les couleurs sont celles de la grille
     (Catppuccin Mocha), ecrites en dur comme le reste du theme : l'app est une
     coque WebView, il n'y a pas de palette Android a heriter. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#1E1E2E" />
    <stroke android:width="1dp" android:color="#313244" />
    <corners android:radius="22dp" />
</shape>
